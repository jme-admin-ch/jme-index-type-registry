<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.admin.bit.jeap</groupId>
        <artifactId>jeap-spring-boot-parent</artifactId>
        <version>38.1.0</version>
        <relativePath/>
    </parent>

    <groupId>ch.admin.bit.jeap.jme</groupId>
    <artifactId>jme-index-type-registry</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>${project.groupId}:${project.artifactId}</name>
    <url>https://github.com/jme-admin-ch/jme-index-type-registry/tree/main</url>

    <scm>
        <url>https://github.com/jme-admin-ch/jme-index-type-registry.git</url>
        <connection>scm:git:git://github.com/jme-admin-ch/jme-index-type-registry.git</connection>
        <developerConnection>scm:git:ssh://github.com:jme-admin-ch/jme-index-type-registry.git</developerConnection>
    </scm>

    <developers>
        <developer>
            <name>BIT</name>
            <email>jeap-community@bit.admin.ch</email>
            <organization>Federal Office of Information Technology, Systems and Telecommunication FOITT</organization>
            <organizationUrl>https://www.bit.admin.ch/</organizationUrl>
        </developer>
    </developers>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <properties>
        <!-- Suppress the default Maven deploy of the registry POM itself — only per-type artifacts are deployed -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <!-- Override with -Dindextype.git.url= to skip master branch comparison locally -->
        <indextype.git.url>https://github.com/jme-admin-ch/jme-index-type-registry</indextype.git.url>
        <jeap-opensearch-index-type-registry-maven-plugin.version>3.0.0</jeap-opensearch-index-type-registry-maven-plugin.version>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Add license entry to jar manifest -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <configuration>
                        <archive>
                            <manifestEntries>
                                <Bundle-License>https://www.apache.org/licenses/LICENSE-2.0</Bundle-License>
                            </manifestEntries>
                        </archive>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- Check third-party license compliance -->
            <plugin>
                <groupId>org.honton.chas</groupId>
                <artifactId>license-maven-plugin</artifactId>
            </plugin>
            <!-- Generate third-party license list -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>io.github.git-commit-id</groupId>
                <artifactId>git-commit-id-maven-plugin</artifactId>
                <configuration>
                    <skipPoms>false</skipPoms>
                </configuration>
            </plugin>
            <plugin>
                <groupId>ch.admin.bit.jeap</groupId>
                <artifactId>jeap-opensearch-index-type-registry-maven-plugin</artifactId>
                <version>${jeap-opensearch-index-type-registry-maven-plugin.version}</version>
                <configuration>
                    <descriptorDirectory>${basedir}/index-types</descriptorDirectory>
                    <basePackage>ch.admin.bit.jme.opensearch.index</basePackage>
                    <gitUrl>${indextype.git.url}</gitUrl>
                    <trunkBranchName>main</trunkBranchName>
                    <indexTypeVersion>${jeap-opensearch-index-type.version}</indexTypeVersion>
                </configuration>
                <executions>
                    <execution>
                        <id>validate-registry</id>
                        <goals>
                            <goal>registry</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>deploy-index-type-artifacts</id>
                        <goals>
                            <goal>deploy-index-type-artifacts</goal>
                        </goals>
                        <configuration>
                            <groupIdPrefix>ch.admin.bit.jeap.jme.indextype</groupIdPrefix>
                            <mavenDeployGoal>deploy</mavenDeployGoal>
                            <deployAllIndexTypes>false</deployAllIndexTypes>
                            <pomTemplateFile>${project.basedir}/indextype.template.pom.xml</pomTemplateFile>
                            <trunkBranchName>main</trunkBranchName>
                            <trunkMavenProfile>maven-central-publish</trunkMavenProfile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
